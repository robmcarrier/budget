name: React CI

on:
  pull_request:
    paths-ignore:
      - './react-ts/**.ts'
      - './react-ts/**.tsx'
      - './react-ts/**.js'
      - './react-ts/**.jsx'
      - './react-ts/**'

jobs:
  build:
    name: React CI
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Java CI build not required"'